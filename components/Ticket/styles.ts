import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  text: {
    paddingTop: 24
  },
  ticketsNumber: {
    fontWeight: 'bold',
    marginBottom: 48
  },
  ticketId: {
    marginTop: 8,
    fontWeight: 'bold'
  }
})
